#!/bin/bash
sudo gem install net-ssh
sudo gem install net-scp
sudo gem install --local specinfra-2.2.0.gem
sudo gem install rspec --version '~> 3.0'
sudo gem install rspec-its
sudo gem install --local serverspec-2.1.0.gem
